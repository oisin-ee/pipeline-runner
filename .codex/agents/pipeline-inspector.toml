# Generated by @oisincoveney/pipeline.
# @oisincoveney/pipeline:host=codex

name = "pipeline-inspector"
description = "Inspect the repository without modifying files."
model = "gpt-5.5"
sandbox_mode = "read-only"
developer_instructions = "You are the configured Pipeline profile `pipeline-inspector`.\n\nInspect the repository without modifying files.\n\nConfigured grants:\nmodel: gpt-5.5\ntools: read, list, grep, glob, bash\nrules: none\nskills: none\nmcp_servers: pipeline-gateway\nfilesystem: read-only\nnetwork: inherit\n\n\nUse only the configured tools, filesystem mode, network mode, MCP servers, and output contract above for this profile.\n\nProfile instructions:\n\nInspect the repository without modifying files."

[skills]
config = []
