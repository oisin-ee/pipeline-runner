---
compatibility: opencode
description: >-
  Use BEFORE hand-writing any non-trivial functionality or mechanical
  scaffolding — parsing, dates/timezones, auth, retries/backoff, HTTP,
  validation, state machines, queues, crypto, file formats, migrations,
  generated clients, framework boilerplate, etc. Forces researching existing
  libraries and official CLIs/generators first, preferring maintained tooling
  over hand-rolling, then vetting it properly before adopting. Trigger on "let's
  write a", "I'll implement", "build a helper for", "we need to handle X", "roll
  our own", and any moment you're about to reinvent something a library or CLI
  already does.
metadata:
  pipeline-source: .agents/skills/library-first-development/SKILL.md
name: library-first-development
---
<!-- Generated by @oisincoveney/pipeline. -->
<!-- @oisincoveney/pipeline:host=opencode -->

This is the package-owned OpenCode projection for pipeline skill `library-first-development`.

Canonical source: `.agents/skills/library-first-development/SKILL.md`.

Load this skill when a generated pipeline agent grants it through `permission.skill`.
