---
compatibility: opencode
description: >-
  Use when implementing any non-trivial feature, behaviour, or bug fix — write
  the failing test first, in vertical tracer-bullet slices, one test → one
  implementation at a time. Tests verify behaviour through public interfaces,
  never implementation details. Trigger on "build X", "implement X", "fix this
  bug", "add a feature", "red-green-refactor", "write tests", and any behaviour
  change that isn't config/docs/rename.
metadata:
  pipeline-source: .agents/skills/test/SKILL.md
name: test
---
<!-- Generated by @oisincoveney/pipeline. -->
<!-- @oisincoveney/pipeline:host=opencode -->

This is the package-owned OpenCode projection for pipeline skill `test`.

Canonical source: `.agents/skills/test/SKILL.md`.

Load this skill when a generated pipeline agent grants it through `permission.skill`.
